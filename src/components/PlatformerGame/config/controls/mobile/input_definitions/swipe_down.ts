// This is the configuration for what defines a "swipe down" and assigns it to swipe_down as a user input that can be assigned controls.

const input_definition = {
    // qualities such as minimum speed, required angle, etc. would be listed here to be configured
};

export default input_definition;