The `config` files define the game. All values related to the game are stored in `config` files and dynamically pulled by the code.

Learn about how the `config` files work by reading the documentation in `docs/dev_notes`.